# Resume Bullets

请仅在亲自运行、理解并能解释实现后使用；不要添加虚构用户量或生产性能数据。

- 设计并实现 Python/FastAPI AI quality platform，以 deterministic local RAG 和 versioned JSONL datasets 对 contract、retrieval、grounding、multi-turn isolation、robustness 进行可重复回归测试。
- 构建 JSON/HTML/JUnit evidence pipeline 与 API-driven dashboard，将 pass rate、recall@k、citation/unsupported claim、p50/p95 latency、error rate 接入 CI quality gates。
- 建立 unit/contract/evaluation/integration 测试分层及 k6/Locust load assets，并通过 ruff、mypy、pytest coverage 与 Docker healthcheck 固化交付质量。
