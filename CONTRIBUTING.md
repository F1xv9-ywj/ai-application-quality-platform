# Contributing

1. 从 issue 开始，说明可验证 acceptance criteria。
2. 创建小而聚焦的分支和变更；不要提交真实用户数据、secrets 或 model credentials。
3. 运行 `make check` 或 `scripts/check.ps1`，在 PR 中粘贴命令与结果摘要。
4. 新 metric 必须给出定义、分母、边界值和 regression case；行为变更必须同步 CHANGELOG。

提交即表示你同意项目按 MIT License 分发，并遵守 [Code of Conduct](CODE_OF_CONDUCT.md)。
